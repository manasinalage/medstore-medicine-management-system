# medstore-medicine-management-system
MedStore is a Spring Boot-based Medicine Management System using PostgreSQL. Admin users can perform CRUD operations on medicines, while users can view and search medicines to check price and available quantity.
